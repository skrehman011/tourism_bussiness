import 'package:flutter/material.dart';

class ItemBooking extends StatelessWidget {
  const ItemBooking({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
